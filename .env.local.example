# Optional. The demo runs fully offline with a deterministic local parser.
# Set this to route parsing through the validated parse_reaction LLM prompt.
# The key stays server-side (used only in src/app/api/parse/route.ts).
ANTHROPIC_API_KEY=sk-ant-...
